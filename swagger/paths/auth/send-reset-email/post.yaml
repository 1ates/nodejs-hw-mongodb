tags:
  - Auth
summary: Send password reset email
operationId: sendResetEmail
description: Send a password reset email to the specified user's email address.
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../../components/schemas/auth.yaml#/SendResetEmailRequest'
responses:
  '200':
    description: Password reset email sent successfully.
  '400':
    description: Bad request.
